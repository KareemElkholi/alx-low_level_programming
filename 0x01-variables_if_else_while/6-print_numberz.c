#include <stdio.h>

/**
 * main - prints the single digit numbers of base 10
 * Return: 0
 */

int main(void)
{
	int ch;

	for (ch = 48; ch <= 57; ch++)
		putchar(ch);
	putchar('\n');

	return (0);
}
